services:
  - type: web
    name: dgr-performance-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dgr_dashboard:server --bind 0.0.0.0:$PORT --workers 1 --timeout 120
